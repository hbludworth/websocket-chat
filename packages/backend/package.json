{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "ISC",
  "scripts": {
    "build": "rm -rf build && tsc",
    "dev": "ts-node-dev -r tsconfig-paths/register ./src/index.ts --respawn --transpileOnly"
  },
  "devDependencies": {
    "@types/node": "^20.2.5",
    "ts-node-dev": "^2.0.0",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.1.3"
  }
}
